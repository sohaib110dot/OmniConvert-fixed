services:
  omniconvert:
    build: .
    container_name: omniconvert
    restart: unless-stopped
    env_file: .env
    environment:
      PORT: "3000"
    ports:
      - "127.0.0.1:3001:3000"
    volumes:
      - ./uploads:/app/uploads
